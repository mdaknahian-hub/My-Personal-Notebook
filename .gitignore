# Gradle files (legacy)
.gradle/
build/

# Local configuration file (sdk path, etc)
local.properties

# Log/OS Files
*.log

# Android Studio generated files and folders
captures/
.externalNativeBuild/
.cxx/
*.aab
*.apk
output-metadata.json

# IntelliJ
*.iml
.idea/
misc.xml
deploymentTargetDropDown.xml
experiment.xml
misc.xml

# Keystore files (Android legacy)
*.jks
# NOTE: দোকানের APK সাইনিং কী `android-signing/*.keystore` ইচ্ছে করে
# রিপোতে রাখা হয় (হারালে অ্যাপ আপডেট করা যাবে না) — রিপো প্রাইভেট রাখুন!
!android-signing/*.keystore

# Google Services (e.g. Firebase)
google-services.json

# Android Profiling
*.hprof

# ---------------- Node / Next.js ----------------
node_modules/
.next/
out/
dist/
coverage/
.turbo/
next-env.d.ts
*.tsbuildinfo

# ---------------- Database (SQLite) ----------------
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm

# ---------------- Environment ----------------
.env
.env.local
.env*.local

# ---------------- APK build (generated) ----------------
twa/
bw-config.json

# ---------------- Misc ----------------
.DS_Store
.env.local
.env*.local
