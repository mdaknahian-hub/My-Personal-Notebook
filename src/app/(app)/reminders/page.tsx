import { RemindersClient } from "@/components/pages/reminders-client";

export const metadata = { title: "তাগাদা কেন্দ্র" };

export default function RemindersPage() {
  return <RemindersClient />;
}
