import { TransactionsClient } from "@/components/pages/transactions-client";

export const metadata = { title: "লেনদেন" };

export default function TransactionsPage() {
  return <TransactionsClient />;
}
